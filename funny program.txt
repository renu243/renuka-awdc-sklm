#include<stdio.h>
#include<string.h>
void main()
{
char str[100],c='a';int i,len=0;
printf("\nenter a string:");
gets(str);
len=strlen(str);
for(i=0;i<len;i++)
{
if(str[i]==c)
{
str[i]='A';
}
}
for(i=0;i<len;i++)
printf("%c",str[i]);
}
output:
enter a string:aditya
AdityA